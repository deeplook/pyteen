from pyteen.snippets.maps.map_search import make_map # , search

def test_map_search():
    m, tx = make_map()
    # tx.value = "Berlin, Germany"
    # search(tx, m) # really needed?
    # assert m.center != [0, 0]
